{
  "name": "graph-keeper",
  "version": "1.0.0",
  "description": "Keeps your GitHub contribution graph green via scheduled commits",
  "main": "index.js",
  "scripts": {
    "test": "jest"
  },
  "dependencies": {
    "@octokit/rest": "^20.0.0"
  },
  "devDependencies": {
    "fast-check": "^3.0.0",
    "jest": "^29.0.0"
  }
}
